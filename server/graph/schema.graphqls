scalar Time
scalar Int64

# ========================
# INTERFACES
# ========================

interface Node {
  id: ID!
}

# ========================
# ENUMS
# ========================

enum Role {
  USER
  ADMIN
  SUPER_ADMIN
}

enum BookingStatus {
  PENDING
  CONFIRMED
  CANCELLED
  CHECKED_IN
  CHECKED_OUT
}

enum RoomStatus {
  AVAILABLE
  OCCUPIED
  MAINTENANCE
  RESERVED
}

enum PaymentStatus {
  PENDING
  COMPLETED
  FAILED
  REFUNDED
}

enum PaymentProvider {
  STRIPE
  PAYSTACK
  FLUTTERWAVE
  PAYPAL
}

# ========================
# CORE TYPES
# ========================

type User implements Node {
  id: ID!
  name: String!
  email: String!
  role: Role!
  isActive: Boolean!
  createdAt: Time!
  updatedAt: Time!
  deletedAt: Time

  reviews: [Review!]
  bookings: [Booking!]
}

type Room implements Node {
  id: ID!
  name: String!
  description: String
  price: Int64!
  status: RoomStatus!

  roomNumber: String!
  amenities: [String!]

  images: [RoomImage!]

  bookings: [Booking!]
  reviews: [Review!]

  createdAt: Time!
  updatedAt: Time!
  deletedAt: Time
}

type RoomImage implements Node {
  id: ID!
  roomId: ID!
  url: String!
  isPrimary: Boolean!
}

type Booking implements Node {
  id: ID!

  user: User!
  room: Room!

  checkInDate: Time!
  checkOutDate: Time!

  status: BookingStatus!
  paymentStatus: PaymentStatus!

  totalPrice: Int64!

  reference: String!

  expiresAt: Time!
  cancelledAt: Time

  nightCount: Int64!
  unitPrice: Int64
  taxAmount: Int64
  discount: Int64

  createdAt: Time!
}

type Review implements Node {
  id: ID!

  user: User!
  room: Room!

  rating: Int!
  comment: String

  createdAt: Time!
}

# ========================
# PAYMENT
# ========================

type Payment implements Node {
  id: ID!

  booking: Booking!

  amount: Int64!

  provider: PaymentProvider!
  transactionReference: String!

  status: PaymentStatus!

  paymentDate: Time!
}

# ========================
# AUTH
# ========================

type AuthPayload {
  accessToken: String!
  refreshToken: String!
  user: User!
}

# ========================
# SYSTEM AUDIT
# ========================

type AuditLog implements Node {
  id: ID!

  userId: ID!
  userEmail: String
  userRole: String

  action: String!
  entity: String!
  entityId: String

  riskLevel: String
  suspicious: Boolean
  reason: String

  createdAt: Time!
}

# ========================
# CONNECTION TYPES
# ========================

type UserConnection {
  users: [User!]!
  total: Int!
  page: Int!
  limit: Int!
}

type RoomConnection {
  rooms: [Room!]!
  total: Int!
  page: Int!
  limit: Int!
}

type BookingConnection {
  bookings: [Booking!]!
  total: Int!
  page: Int!
  limit: Int!
}

type AuditLogConnection {
  logs: [AuditLog!]!
  total: Int!
  page: Int!
  limit: Int!
}

# ========================
# INPUT TYPES
# ========================

input RegisterInput {
  name: String!
  email: String!
  password: String!
}

input LoginInput {
  email: String!
  password: String!
}

input CreateRoomInput {
  name: String!
  description: String
  price: Int64!
  roomNumber: String!
  amenities: [String!]
}

input UpdateRoomInput {
  id: ID!
  name: String
  description: String
  price: Int64
  status: RoomStatus
  roomNumber: String
  amenities: [String!]
}

input CreateBookingInput {
  roomId: ID!
  checkInDate: Time!
  checkOutDate: Time!
}

input MakePaymentInput {
  bookingId: ID!
  amount: Int64!
  provider: PaymentProvider!
}

input CreateReviewInput {
  roomId: ID!
  rating: Int!
  comment: String
}

input PaginationInput {
  page: Int = 1
  limit: Int = 20
}

input AuditLogFilterInput {
  userId: ID
  action: String
  entity: String
  riskLevel: String
  suspicious: Boolean
  startDate: Time
  endDate: Time
}

# ========================
# QUERIES
# ========================

type Query {
  # AUTH
  me: User!

  # ROOMS
  room(id: ID!): Room
  rooms(status: RoomStatus, pagination: PaginationInput): RoomConnection!

  roomAvailability(
    roomId: ID!
    checkInDate: Time!
    checkOutDate: Time!
  ): Boolean!

  # BOOKINGS
  myBookings(pagination: PaginationInput): BookingConnection!
  booking(id: ID!): Booking!

  # ADMIN
  users(pagination: PaginationInput): UserConnection!
  user(id: ID!): User
  adminBookings(pagination: PaginationInput): BookingConnection!

  # SUPER ADMIN
  allBookings(pagination: PaginationInput): BookingConnection!

  # AUDIT LOGS
  auditLogs(
    filter: AuditLogFilterInput
    pagination: PaginationInput
  ): AuditLogConnection!
}

# ========================
# MUTATIONS
# ========================

type Mutation {

  # AUTH
  register(input: RegisterInput!): AuthPayload!
  login(input: LoginInput!): AuthPayload!

  # CUSTOMER
  createBooking(input: CreateBookingInput!): Booking!
  cancelBooking(id: ID!): Booking!
  makePayment(input: MakePaymentInput!): Payment!
  createReview(input: CreateReviewInput!): Review!

  # ADMIN
  createRoom(input: CreateRoomInput!): Room!
  updateRoom(input: UpdateRoomInput!): Room!
  updateRoomStatus(roomId: ID!, status: RoomStatus!): Room!
  confirmBooking(id: ID!): Booking!

  # SUPER ADMIN
  approveRoom(roomId: ID!): Room!
  rejectRoom(roomId: ID!): Room!
  promoteToAdmin(userId: ID!): User!
  deactivateUser(userId: ID!): User!
}